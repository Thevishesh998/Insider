import React, { useState } from "react";
const questions = [["How do I apply?", "Open a role and select Apply. Sign in first, then keep your resume current so your application is ready to submit."], ["How does resume matching work?", "When you update a PDF resume, the platform uses its listed skills to surface relevant roles first."], ["Can freshers apply?", "Yes. Review the experience level on each role and apply to opportunities that match your current skills."], ["How do recruiters contact candidates?", "Recruiters review applications submitted through the platform and contact candidates using their application details."], ["How do I update my resume?", "Open Profile or My jobs and upload a new PDF resume. Your latest file replaces the previous one."]];
const Faq = () => { const [open, setOpen] = useState(0); return <section className="mx-auto max-w-4xl px-4 py-16 sm:px-6 lg:px-8"><div className="text-center"><p className="text-sm font-bold uppercase tracking-[0.16em] text-blue-600">Candidate help</p><h2 className="mt-3 text-3xl font-bold tracking-tight text-slate-900">Frequently asked questions</h2></div><div className="mt-8 divide-y divide-slate-200 rounded-2xl border border-slate-200 bg-white px-5 shadow-sm">{questions.map(([question, answer], index) => <div key={question}><button onClick={() => setOpen(open === index ? -1 : index)} className="flex w-full items-center justify-between gap-4 py-5 text-left text-sm font-bold text-slate-900" aria-expanded={open === index}><span>{question}</span><span className="text-xl font-normal text-blue-600">{open === index ? "−" : "+"}</span></button>{open === index && <p className="max-w-3xl pb-5 text-sm leading-6 text-slate-600">{answer}</p>}</div>)}</div></section>; };
export default Faq;
